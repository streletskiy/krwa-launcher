# KRWA Launcher

Открытый лаунчер Minecraft для сервера KRWA. Он устанавливает подходящую Java, Minecraft, NeoForge и файлы модпака, проверяет их контрольные суммы, запускает игру и обновляет себя без ручной настройки клиента.

Текущая версия лаунчера — **0.1.12**. Основной профиль — **KRWA Aeronautics 1.0.2** на Minecraft 1.21.1 и NeoForge 21.1.248.

## Возможности

- автоматическая установка и проверка Java, Minecraft, NeoForge, библиотек и модов;
- единый профиль Create Aeronautics с обязательными и необязательными модулями;
- собственная Yggdrasil-авторизация через AsterYggdrasil;
- регистрация, восстановление доступа, смена пароля и загрузка скина внутри лаунчера;
- русский, английский и польский интерфейс с автоопределением языка системы;
- безопасное добавление сервера в `servers.dat` без удаления пользовательских записей;
- автообновление Windows- и Linux-сборок;
- отображение этапов подготовки, реального прогресса и числа загружаемых файлов.

Лаунчер предназначен для инфраструктуры KRWA, но его исходники можно использовать как пример глубокой адаптации Helios под собственный сервер и нестандартный Yggdrasil.

## Структура репозитория

| Путь | Назначение |
|---|---|
| `launcher-src` | Electron-приложение, интерфейс, тесты и конфигурация сборки |
| `nebula-src` | генератор `distribution.json` |
| `repository` | метаданные и статические файлы клиентского профиля |
| `community` | небольшой сервис публичного каталога и постоянных ссылок на загрузки |
| `scripts` | импорт и проверка профиля NeoForge |
| `compose.yml`, `Caddyfile` | воспроизводимый локальный backend для разработки |

Базы аккаунтов, ключи, резервные копии, сведения о хостах и внутренняя топология сети в Git не хранятся.

## Быстрый старт для разработки

Требуются Node.js 22 и npm. Для запуска интерфейса:

```powershell
Set-Location launcher-src
npm ci
node --test tests/*.test.cjs
npm run lint
npm start
```

Команда `npm start` запускает Electron в режиме разработки. Настройки публичного сервиса находятся в `launcher-src/app/assets/js/distromanager.js`, `launcher-src/app/assets/js/authservice.js`, `launcher-src/package.json` и `launcher-src/electron-builder.yml`.

## Сборка

Сборка для текущей платформы выполняется из `launcher-src`:

```powershell
npm ci
npm run dist
```

В корне также есть вспомогательные команды:

- `build-launcher.cmd` — Windows x64 NSIS и Linux x64 AppImage;
- `publish-launcher.cmd` — копирование готовых артефактов в `repository/downloads`;
- `release-launcher.cmd <version>` — обновление версии, сборка и подготовка файлов канала обновлений;
- `rebuild-distribution.cmd` — пересборка `repository/distribution.json` через Nebula.

Windows-сборка пока не подписана сертификатом издателя, поэтому SmartScreen может показать предупреждение. Перед выпуском от другого издателя следует настроить code signing и убрать `signAndEditExecutable: false` из `electron-builder.yml`.

## Клиентский профиль

Профиль `repository/servers/krwa-aeronautics-1.21.1` основан на All of Create – Aeronautics 2.5. Версии и upstream-адреса модулей зафиксированы в `neoforge-lock.json`; загруженные файлы проверяются по размеру и контрольной сумме. Лаунчер удаляет только ранее управляемые им файлы и сохраняет пользовательские дополнения.

Изменение профиля выполняется так:

1. Обновить исходный manifest и overrides через `scripts/import-neoforge-curseforge-pack.ps1`.
2. Проверить совместимость, зависимости и условия распространения каждого компонента.
3. Повысить версию в `servermeta.json`.
4. Запустить `rebuild-distribution.cmd`.
5. Выполнить полную проверку загрузки и реальный запуск клиента.

Сторонние моды, ресурспаки и шейдеры сохраняют собственные лицензии и условия распространения.

## Происхождение

Проект основан на двух MIT-проектах Daniel Scalzi:

- [Helios Launcher](https://github.com/dscalzi/HeliosLauncher), исходная точка — `86e4316b963b54ff052be9ec80f316b8f842cf87`;
- [Nebula](https://github.com/dscalzi/Nebula), исходная точка — `7ffc978727b95e03ae9d125688cf5cf132b78419`.

Точные точки импорта и сведения о сторонних компонентах перечислены в [UPSTREAM.md](UPSTREAM.md). Исходные лицензии сохранены в `launcher-src/LICENSE.txt` и `nebula-src/LICENSE`.

Основные отличия KRWA от Helios: полностью переработанный интерфейс, три языка, один управляемый профиль, интеграция с AsterYggdrasil, нативные формы аккаунта и скина, безопасная работа с `servers.dat`, собственный индикатор подготовки и канал тихих обновлений.

## Безопасность и приватные данные

- Не добавляйте в Git `.env`, `auth-data`, резервные копии, токены, приватные ключи и собранные установщики.
- Используйте `.env.example` только как шаблон; реальные значения должны оставаться локальными.
- Для внешнего доступа к авторизации и файлам обязателен HTTPS.
- Перед публикацией форка проверьте все URL, название приложения, `appId`, update feed и условия распространения файлов модпака.

## Лицензия

Код KRWA Launcher распространяется по лицензии [MIT](LICENSE). У vendored-кода и игровых ресурсов могут быть отдельные лицензии; они указаны рядом с соответствующими компонентами или в [UPSTREAM.md](UPSTREAM.md).
